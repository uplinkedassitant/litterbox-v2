[package]
name = "litterbox-v2"
version = "0.2.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
pinocchio = "=0.9.3"
pinocchio-pubkey = "=0.3.0"
pinocchio-system = "=0.4.0"
pinocchio-tkn = "=0.2.2"

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1
